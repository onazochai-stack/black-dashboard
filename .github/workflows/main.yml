cp /sdcard/Download/omega-install-america.tar.gz ~/
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
rm -rf *
tar -xzf ~/omega-install-america.tar.gz
git add .
git commit -m "OMEGA v1 — Full system, 267 files, 4 apps"
git push origin main

tar -xzf omega-complete.tar.gz
pnpm install
pnpm --filter @workspace/api-server run dev
pnpm --filter @workspace/lead-system run devindex.tsApp.tsxomega-complete.tar.gzomega-complete.tar.gz
├── artifacts/api-server/src/          ← All routes + scrapers (Express)
│   ├── app.ts, index.ts
│   ├── routes/ (20 route files)
│   └── scrapers/ (10 scraper modules)
├── artifacts/lead-system/src/         ← Main OMEGA UI (React, 4,366 lines)
│   ├── App.tsx
│   └── components/ (all panels)
├── artifacts/install-america/src/     ← Customer website
├── artifacts/command-center/src/      ← Dashboard app
├── package.json
└── pnpm-workspace.yamlpackage.jsonpnpm-workspace.yamlApp.tsx